# Required for POST /api/study-response (validation study submissions on Vercel or local).
# Create project at https://supabase.com → Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: remote Python predict service (see root README)
# PREDICT_API_URL=
# PREDICT_API_KEY=
